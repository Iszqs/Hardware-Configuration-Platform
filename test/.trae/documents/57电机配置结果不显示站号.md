# 57电机配置结果不显示站号

## Summary
在硬件配置页面的配置结果卡片中，57电机（device_type_id=2）不显示站号配置结果，因为57电机不配置站号。

## Current State Analysis

**配置结果卡片** (`OneClickConfig.vue` L64-83)：
```vue
<div v-if="lastResult" class="result-card" :class="'result-' + lastResult.status">
  ...
  <div class="result-params" v-if="lastResult.status !== 'in_progress'">
    <span class="result-param">站号: {{ lastResult.station_number }}</span>
    <span class="result-param">波特率: {{ lastResult.baud_rate }}</span>
  </div>
</div>
```

**57电机标识**：
- `device_type_id = 2` 对应57电机
- `category_id = 1` 对应立三电机

## Proposed Changes

### 修改 `result-params` 显示逻辑

**文件**: `OneClickConfig.vue` L79-82

**当前代码**：
```vue
<div class="result-params" v-if="lastResult.status !== 'in_progress'">
  <span class="result-param">站号: {{ lastResult.station_number }}</span>
  <span class="result-param">波特率: {{ lastResult.baud_rate }}</span>
</div>
```

**修改为**：
```vue
<div class="result-params" v-if="lastResult.status !== 'in_progress'">
  <span v-if="lastResult.device_type_id !== 2" class="result-param">站号: {{ lastResult.station_number }}</span>
  <span class="result-param">波特率: {{ lastResult.baud_rate }}</span>
</div>
```

**原因**：57电机(device_type_id=2)不配置站号，所以不显示站号行。

## Verification Steps

1. 配置35电机 → 显示"站号"和"波特率"
2. 配置57电机 → 只显示"波特率"，不显示"站号"
