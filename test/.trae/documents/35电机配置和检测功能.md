# 35电机配置和检测按钮功能

## Summary
为35电机实现配置和检测功能。35电机需要同时写入站号和波特率，并验证写入结果；失败时切换站号重试。

## 35电机配置流程（用户需求）

1. **初始尝试**：使用当前站号（默认为1）及波特率（依次为115200、9600）向设备写入数据库中的新站号和新波特率
2. **验证**：若收到响应，判断返回的站号与波特率是否与写入值一致
3. **保存**：若一致，执行断电保存操作（0x00DC写入1）
4. **重试策略**：若步骤失败，依次切换站号2至30，每个站号分别尝试波特率9600和115200，有响应则发送断电保存

## Current State Analysis

**现有代码结构**：

| 层级 | 文件 | 说明 |
|------|------|------|
| 后端服务 | `backend/services/modbus_service.py` | 已有 `write_config_57`, `inspect_57` 方法 |
| 后端路由 | `backend/routers/modbus.py` | 已有57电机API路由 |
| 前端API | `src/api/index.js` | 已有 `writeModbusConfig57`, `inspectModbus57` |
| 前端业务 | `src/views/OneClickConfig.vue` | `configSingle` 和 `inspectSingle` 调用57电机API |

**关键差异**：

| 功能 | 57电机 | 35电机 |
|------|--------|--------|
| 写入站号 | 否 | 是 |
| 写入波特率 | 是 | 是 |
| 验证返回 | 否 | 是（验证站号+波特率） |
| 断电保存 | 是 | 是 |
| 重试策略 | 切换波特率 | 切换站号+波特率 |

## Proposed Changes

### 1. 后端：添加 `write_config_35` 方法

**文件**: `backend/services/modbus_service.py`

**实现逻辑**：
```python
def write_config_35(self, current_station: int, target_station: int, target_baud_rate: int) -> dict:
    """
    35电机配置：
    1. 使用当前站号(current_station)和波特率(115200/9600)尝试写入目标站号和目标波特率
    2. 验证返回的站号和波特率是否与写入值一致
    3. 若一致，执行断电保存
    4. 若失败，切换站号2-30重试
    """
    baud_val = BAUD_RATE_MAP.get(target_baud_rate)
    if baud_val is None:
        return {'success': False, 'message': f'不支持的波特率: {target_baud_rate}'}

    # 尝试写入并验证
    # 第一轮：使用传入的当前站号尝试
    # 第二轮：站号2-30，每站号尝试9600和115200
```

**寄存器地址**（与57相同）：
- 站号寄存器：0x0066
- 波特率寄存器：0x0009
- 断电保存：0x00DC

**字节序**（与57相同）：
- 站号寄存器：小端序 `H`
- 波特率寄存器：大端序 `>H`

### 2. 后端：添加 `inspect_35` 方法

**文件**: `backend/services/modbus_service.py`

**实现逻辑**：读取并验证35电机的站号和波特率

### 3. 后端：添加API路由

**文件**: `backend/routers/modbus.py`

```python
@router.post('/write-config-35')
async def write_config_35(data: dict):
    """35电机配置：写入站号和波特率"""
    current_station = data.get('current_station', 1)
    target_station = data.get('target_station', 1)
    target_baud_rate = data.get('target_baud_rate', 115200)
    return modbus_service.write_config_35(current_station, target_station, target_baud_rate)

@router.post('/inspect-35')
async def inspect_35(data: dict):
    """35电机检测：验证站号和波特率"""
    station = data.get('station_number', 1)
    baud_rate = data.get('baud_rate', 115200)
    return modbus_service.inspect_35(station, baud_rate)
```

### 4. 前端：添加API方法

**文件**: `src/api/index.js`

```javascript
writeModbusConfig35(data) { return request('/api/modbus/write-config-35', { method: 'POST', body: data }) },
inspectModbus35(data) { return request('/api/modbus/inspect-35', { method: 'POST', body: data }) },
```

### 5. 前端：修改 `configSingle` 和 `inspectSingle`

**文件**: `src/views/OneClickConfig.vue`

修改 `configSingle(idx)`:
- 根据 `device_type_id` 判断是35还是57电机
- 35电机(device_type_id=1)：调用 `writeModbusConfig35`，传递 `current_station`, `target_station`, `target_baud_rate`
- 57电机(device_type_id=2)：调用 `writeModbusConfig57`

修改 `inspectSingle(idx)`:
- 35电机：调用 `inspectModbus35`
- 57电机：调用 `inspectModbus57`

## Data Flow

**35电机配置数据流**：
```
前端 configSingle(idx)
  → api.writeModbusConfig35({ current_station, target_station, target_baud_rate })
    → POST /api/modbus/write-config-35
      → modbus_service.write_config_35(current_station, target_station, target_baud_rate)
        1. 尝试用 current_station + 115200/9600 写入 target_station + target_baud_rate
        2. 验证响应
        3. 失败则切换站号2-30重试
        4. 成功则断电保存
```

## Verification Steps

1. 35电机配置：配置成功后卡片边框变绿
2. 35电机配置失败：卡片边框变红
3. 35电机检测：验证站号和波特率
4. 57电机配置/检测：功能保持不变
