# 任务计划：硬件配置面板空状态优化

## 一、现状

硬件配置界面的右侧面板在未执行任何操作时，显示一个完全空白的 `div.empty-result`，没有任何提示信息，视觉上显得突兀。

## 二、目标

在空状态下展示默认使用说明，指导用户正确操作。

## 三、方案

### 修改文件

`src/views/OneClickConfig.vue`

### 具体改动

1. **替换 `empty-result` 的内容** — 将当前空的 `<div class="empty-result"></div>` 替换为包含说明信息的面板：

```
┌─────────────────────────────────┐
│  📋 使用说明                      │
│                                 │
│  ┌─────────────────────────────┐│
│  │  ①  先执行「配置」，再执行「检测」 ││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │  ⚡ 57电机                    ││
│  │  仅支持波特率配置             ││
│  │  站号需通过手动拨码设置       ││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │  ⚡ 35电机                    ││
│  │  站号和波特率均支持配置       ││
│  └─────────────────────────────┘│
└─────────────────────────────────┘
```

2. **展示规则** — 仅在 `!lastResult && !lastInspectResult` 时显示（已经由 `v-if` 控制，无需额外逻辑）

3. **样式** — 卡片风格，步骤编号、设备标签分组，与现有 Sentry 深色主题一致

## 四、不受影响的部分

- 执行配置/检测后，说明信息自动被结果替换（`v-if` 逻辑不变）
- 不影响原有配置/检测功能
- 不涉及后端改动

---

**请审阅，确认后回复"执行"。**
